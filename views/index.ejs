<!DOCTYPE html>
<html>
  <head>
    <title>Users</title>
    <link rel='stylesheet' href='style.css' />
  </head>
  <%- include('./header') %>
  <body>
    <ul>
      <% clents.forEach((clent) => { %>
        <li>
          <h2>
            <%= clent.name %>
          </h2>
          <p>
            <%= clent.email %>
          </p>
        </li>
      <% }) %>
    </ul>

    <a href="/add"><button>Add</button></a>
  </body>
</html>
